#=============================================================================
# ORCP Flink 1.17.2 configuration template.
#
# Placeholders replaced by deploy/centos/30_install_flink.sh at install time:
#   __JM_HOST__         -> NODE_1_HOST (the JobManager host)
#   __CHECKPOINT_DIR__  -> /data/flink/checkpoints
#   __SAVEPOINT_DIR__   -> /data/flink/savepoints
#
# Edit this file, not the rendered one on the servers -- run
# 30_install_flink.sh again to re-render.
#=============================================================================

# ----- JobManager -----------------------------------------------------------
jobmanager.rpc.address: __JM_HOST__
jobmanager.rpc.port: 6123
jobmanager.bind-host: 0.0.0.0
jobmanager.memory.process.size: 2g
rest.address: __JM_HOST__
rest.bind-address: 0.0.0.0
rest.port: 8081

# ----- TaskManager ----------------------------------------------------------
taskmanager.bind-host: 0.0.0.0
taskmanager.host: 0.0.0.0
taskmanager.memory.process.size: 4g
taskmanager.numberOfTaskSlots: 4

parallelism.default: 2

# ----- State + checkpoints --------------------------------------------------
state.backend: rocksdb
state.backend.incremental: true
state.checkpoints.dir: file://__CHECKPOINT_DIR__
state.savepoints.dir: file://__SAVEPOINT_DIR__

execution.checkpointing.interval: 60 s
execution.checkpointing.mode: EXACTLY_ONCE
execution.checkpointing.min-pause: 30 s
execution.checkpointing.timeout: 10 min
execution.checkpointing.externalized-checkpoint-retention: RETAIN_ON_CANCELLATION

# ----- Restart strategy -----------------------------------------------------
restart-strategy: fixed-delay
restart-strategy.fixed-delay.attempts: 10
restart-strategy.fixed-delay.delay: 30 s

# ----- Prometheus metric reporter ------------------------------------------
# The reporter jar (flink-metrics-prometheus-<version>.jar) is copied from
# opt/ to lib/ by 30_install_flink.sh. The port range lets JM and multiple
# TMs on the same host bind without conflict.
metrics.reporters: prom
metrics.reporter.prom.factory.class: org.apache.flink.metrics.prometheus.PrometheusReporterFactory
metrics.reporter.prom.port: 9250-9260

# ----- JVM ------------------------------------------------------------------
env.java.opts.all: "-XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8"

# ----- Classloading ---------------------------------------------------------
# child-first avoids version clashes between the cluster lib/ and job fat jars
# for user-supplied libraries (e.g. Jackson).
classloader.resolve-order: child-first
