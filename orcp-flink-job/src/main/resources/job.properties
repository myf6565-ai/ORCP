# Default parameters for orcp-flink-job.
# Override at submit time with: flink run ... orcp-flink-job.jar --key value
#
# Do NOT commit real credentials.  The values below are placeholders that
# MUST be overridden by the submit script (scripts/submit_job.sh) or the
# orcp-admin submit REST call (Stage F).

# ---- Kafka source ---------------------------------------------------------
kafka.bootstrap=node-1:9092,node-2:9092,node-3:9092
kafka.mid.topic=orcp.mid.events
kafka.group.id=orcp-flink-job

# ---- MySQL dim (orcp_detail.t_customer) ----------------------------------
mysql.url=jdbc:mysql://node-1:3306/orcp_detail?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
mysql.user=orcp_ro
mysql.password=ChangeMe_ro_1!

# ---- OceanBase sink (orcp_dw.agg_order_1min) -----------------------------
# See sql/03_sink_oceanbase.sql for why the URL prefix is 'jdbc:mysql://'
# even though the driver is com.oceanbase.jdbc.Driver.
ob.url=jdbc:mysql://ob-host:2881/orcp_dw?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
ob.user=orcp_rw@tenant#cluster
ob.password=ChangeMe_ob_1!
