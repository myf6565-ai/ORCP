# Default parameters; overridden at submit time via --key value pairs.
# Do NOT commit real credentials; production values are injected by
# the operator (env vars or Nacos-managed config) in Stage E.

kafka.bootstrap=node-1:9092,node-2:9092,node-3:9092

mysql.url=jdbc:mysql://node-1:3306/orcp_detail?useSSL=false&serverTimezone=Asia/Shanghai
mysql.user=orcp_ro
mysql.password=changeme

ob.url=jdbc:oceanbase://ob-host:2883/orcp_dw?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
ob.user=orcp_rw@tenant#cluster
ob.password=changeme
