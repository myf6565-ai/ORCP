# 假设与未决项

## 合理默认假设

1. MVP 使用 Kafka 单节点（本地开发）。
2. Flink 作业先以独立 Jar 提交运行，不内嵌服务。
3. query-service 先使用内存缓存结果，后续可替换 Redis。
4. 规则聚合默认阈值为 3（可通过参数调整）。
5. 暂不接入外部鉴权系统，gateway-service 提供占位能力。

## 未决项

1. 生产环境 Flink 集群规格与资源限制。
2. 结果持久化方案（Redis / OLAP / 时序库）。
3. 规则模型是否支持 DSL 与动态热更新。
